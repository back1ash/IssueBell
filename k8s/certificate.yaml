apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: issuebell-com-tls
  namespace: gateway
spec:
  secretName: issuebell-com-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - issuebell.com
  - "*.issuebell.com"  # 와일드카드도 무료로 발급 (DNS-01만 가능)
