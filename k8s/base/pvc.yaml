apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: issuebell
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
